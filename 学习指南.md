# 迷你采购智能体 · 完整学习与上手指南

> 目标：通过 8 个递进的关卡，在本地从零徒手写出一个「**迷你采购智能体**」。
> 它是西高院采购智能化项目的**微缩版**——每一关都对标真项目里的一个技术点
> （MaaS 模型网关、制度知识库 RAG、ReRank 重排、征信风控、单一来源审核、
> 工作流编排、权限过滤、效果评测）。每关都能独立跑起来，边写边学，
> 最后拼成一个能"查供应商、答制度、审风险、写单一来源理由"的完整原型。
>
> 学完这套，你再看西高院那份技术方案，会从"很多名词看不懂"变成"每个模块我都亲手写过微缩版"。

---

## 零、先把两个东西对上号：真项目 vs 你要造的练手版

西高院方案是一栋"四层楼"，你在本地按同样的四层造一个小的：

| 真项目（西高院） | 你的迷你版（本地 Mac 就能跑） | 在第几关造 |
|---|---|---|
| ④ 业务场景：风险审核/推荐/单一来源/助手 | 命令行里的采购助手 + 三个采购场景 | 第 1、8 关 |
| ③ 智能体平台：配置/工作流/提示词/工具/权限 | 你手写的 harness + skills + 工作流 + 权限过滤 | 第 3、4、8 关 |
| ② 数据与知识：供应商库/制度RAG/征信接口 | `采购数据/` 下的 JSON + 制度 md + 假征信 | 第 2、6、7 关 |
| ① 模型底座：千问/Embedding/OCR + MaaS网关 | Ollama 的 qwen + bge-m3 + 你写的模型网关 | 第 1、3、6 关 |

> **一句话**：真项目里那些要花钱买、要部署服务器的东西（MaaS 网关、大模型、向量库、征信 API），
> 你在本地都用一个"轻量替身"亲手写一遍。原理完全一样，只是规模小、不要钱。

---

## 一、先建立全景：一个采购智能体由哪些部分组成

动手前花 20 分钟读懂这张"零件清单"。后面每一关就是亲手造其中一个零件。

```
┌────────────────────────────────────────────────────────────┐
│  采购智能体运行时（你的 Harness ≈ 真项目的"智能体平台"）        │
│                                                            │
│   ┌─────────┐   循环: 想 → 调工具 → 看结果 → 再想            │
│   │  千问LLM │◄────────────────────────────────┐          │
│   └────┬────┘   （所有模型调用都走"模型网关"）      │          │
│        │ 决定：查库？搜制度？调征信？               │          │
│        ▼                                        │          │
│   ┌────────────────────────────────────┐        │          │
│   │ 工具 / 数据层                        │────────┘          │
│   │  · 查供应商库（供应商.json）           │  结果回填上下文     │
│   │  · 制度检索 RAG（制度/*.md）           │                   │
│   │  · 征信查询 / 涉诉名单（外部数据）       │  ← MCP 标准接口    │
│   │  · 供应商评分（结构化规则）             │                   │
│   └────────────────────────────────────┘                   │
│                                                            │
│   ┌──────────┐  ┌──────────┐  ┌───────────────┐            │
│   │ Skills   │  │ Memory   │  │ Context 管理    │            │
│   │ 单一来源  │  │ 会话态    │  │ 压缩历史/控预算  │            │
│   │ 理由技能等│  │ 用户身份  │  │                │            │
│   └──────────┘  └──────────┘  └───────────────┘            │
│                                                            │
│   ┌──────────────────┐  ┌──────────────────┐               │
│   │ 护栏 Guardrails   │  │ 评测 Evals/日志    │               │
│   │ 权限过滤/人工确认   │  │ 命中率/可追溯      │               │
│   └──────────────────┘  └──────────────────┘               │
└────────────────────────────────────────────────────────────┘
```

### 核心术语速查表（每个都标了"真项目里叫什么"）

| 术语 | 一句话解释 | 真项目对应 | 第几关 |
|---|---|---|---|
| **LLM / messages** | 和大模型对话的协议：system/user/assistant 消息数组 | 千问文本模型 | 1 |
| **MaaS / 模型网关** | 所有模型调用的唯一入口：路由、限流、重试、日志、用量统计 | MaaS/模型网关（方案头号重点） | 3 |
| **Tool / Function Calling** | 模型只输出"我想调 XX 工具+参数"，你的代码执行后把结果喂回 | 工具集管理 | 2 |
| **Agent Loop（ReAct）** | while 循环：想→调工具→看结果→再想，直到给答案 | 智能体运行 | 2 |
| **Harness（运行时/骨架）** | 承载 agent 循环的工程：工具注册、护栏、日志、上下文管理 | 智能体定制平台 | 3 |
| **Skill（技能包）** | 一份"何时用、怎么做"的说明书，按需加载（渐进式披露） | 提示词管理/业务提示词 | 4 |
| **Memory / 上下文管理** | 短期=会话历史；长期=写文件跨会话；压缩防爆窗口 | Agent 会话态/Chat历史 | 5 |
| **Embedding / 向量检索** | 把文字变数字向量，语义相近的向量也相近，用于"按意思搜" | Qwen3-Embedding / BGE-M3 | 6 |
| **RAG（检索增强生成）** | 先检索制度片段，再让模型照着片段带出处回答 | 知识库 + 检索策略 | 6 |
| **ReRank（重排序）** | 对检索出的候选片段二次精排，提升命中准确率 | Qwen3-Reranker / BGE-reranker | 6 |
| **混合检索** | 关键词检索 + 向量检索融合，兼顾"文号/供应商名"精确匹配 | RAG 检索策略 | 6 |
| **可追溯 / 引用溯源** | 回答必须给出命中的文档名、条款、来源、时间 | 答案可追溯（合规硬要求） | 6 |
| **MCP** | 让工具以标准协议提供给任意 agent，"AI 界的 USB 接口" | 工具集管理 / MCP Server | 7 |
| **外部数据接入** | 对接征信/司法/舆情，区分"外部事实"与"AI 建议" | 供应商风险评估 AI 插件 | 7 |
| **工作流编排** | 把"查数据→判断→检索→生成→回填"多步串起来，不是单轮问答 | 工作流编排模块 | 8 |
| **权限 / 数据隔离** | 检索时先按角色/单位过滤，再召回，避免越权看数据 | 权限与安全模块 | 8 |
| **Evals（评测）** | 用固定测试集+打分衡量效果，是调优和验收的唯一可靠依据 | 效果评测与验收指标 | 8 |

### 推荐阅读（每关前读对应一篇即可，别囤积）

- Anthropic《Building effective agents》——什么时候该用 agent、什么时候工作流就够（第 2 关前）
- Anthropic《Agent Skills》介绍——skill 设计哲学（第 4 关前）
- 任一篇讲 RAG 的中文综述，重点看"切分/检索/重排"三段（第 6 关前）
- MCP 官方文档 modelcontextprotocol.io 的 Quickstart（第 7 关前）

---

## 二、环境准备（第 0 关，约 30 分钟）

你机器现状：macOS，系统 Python 3.9（偏旧），Homebrew 已装，Ollama 未装。

```bash
# 1. 安装 uv（现代 Python 管理器，自动装新版 Python，替代 pip+venv）
brew install uv

# 2. 安装 Ollama（本地跑开源模型，对标真项目的"私有化部署"）
brew install ollama
brew services start ollama          # 后台常驻，监听 127.0.0.1:11434

# 3. 拉一个支持工具调用的中文模型（约 5GB，对标真项目的"千问"）
ollama pull qwen3:8b

# 4. 拉 embedding 模型（第 6 关制度检索用，对标"Qwen3-Embedding/BGE-M3"）
ollama pull bge-m3

# 5. 验证
ollama run qwen3:8b "你好，用一句话介绍你自己"

# 6. 初始化本项目依赖
cd agent-lab
uv sync
```

> **为什么用 Ollama**：它暴露 OpenAI 兼容 API（`http://localhost:11434/v1`）。
> 你写的所有代码，将来到客户现场只需把 base_url 换成他们 MaaS 网关（或昇腾 MindIE）的地址就能复用，
> 练习不会白费——这正是"私有化部署接口标准化"的价值。
>
> **如果 8B 太慢**：换 `ollama pull qwen3:4b`，工具调用能力略弱但够练手。

---

## 三、八个关卡（迷你采购智能体主线）

每关详细任务卡在 `stageN_*/README.md`。标 ⭐ 的是核心关卡，也是西高院方案里最吃重的部分。

### 第 1 关 · 采购智能助手雏形（半天）
**造的零件**：LLM 对话 / messages / 流式 ｜ **对标**：采购智能助手
连上本地千问，做一个能多轮对话的"采购助手"命令行程序。起步代码已给（`stage1_chat/chat.py`），
先跑通再逐行读懂，完成 3 个改造练习。
**你将真正理解**：模型是无状态的，"记得上文"只是每次把全部历史重发一遍。

### 第 2 关 ⭐ · 徒手写 Agent 循环 + 查供应商库（一个周末，最重要）
**造的零件**：tool calling / ReAct 循环 ｜ **对标**：工具集 + 供应商信息库
不用任何框架，手写 agent 循环，给模型三个工具：`查供应商(名称/品类)`、`列出供应商`、`看供应商详情`。
问它"帮我找能供高压开关的供应商"，看它自己决定调哪个工具。
**你将真正理解**：function calling 协议；模型"自主决策"就是循环里一次次生成；工具描述写好坏直接决定智商。

### 第 3 关 ⭐ · 把循环重构成 Harness + MaaS 模型网关（3 个晚上）
**造的零件**：工具注册器 / 护栏 / 日志 / 模型网关 ｜ **对标**：MaaS/模型网关（方案头号重点）
把第 2 关重构成小型 harness，并**单独抽出一个"模型网关"模块**：所有模型调用都走它，
统一做模型路由（简单问答走小模型、复杂分析走大模型）、超时重试、限流、调用日志、用量统计。
**你将真正理解**：为什么方案反复强调"业务不直连模型，一律走网关"；agent 产品 80% 工程量在 harness。

### 第 4 关 ⭐ · 提示词与技能管理（Skills）（2 个晚上）
**造的零件**：技能包 / 渐进式披露 ｜ **对标**：提示词管理 / 业务提示词
实现技能机制：每个技能一个 `SKILL.md`（frontmatter 写"何时用" + 正文写步骤），
平时只注入名称+描述，用到时才 `load_skill` 加载全文。写两个真技能：
`单一来源理由撰写`、`供应商背调要点`。
**你将真正理解**：skill 与 tool 的区别（tool 给能力，skill 给知识和流程）；为什么不把所有提示词塞进 system prompt。

### 第 5 关 · 会话记忆与上下文管理（2 个晚上）
**造的零件**：上下文压缩 / 长期记忆 ｜ **对标**：Agent 会话态 / Chat 历史
实现上下文压缩（对话太长时自动总结旧历史）和长期记忆（记住"我是哪个单位的采购员"，重启仍记得）。
**你将真正理解**：所谓"记忆"全是工程手段，模型本身过目即忘；上下文预算是真金白银。

### 第 6 关 ⭐ · 制度知识库 RAG（一个周末，客户项目直接相关）
**造的零件**：切分 / Embedding / 混合检索 / ReRank / 引用溯源 ｜ **对标**：知识库 + RAG 可追溯
用 `采购数据/制度/` 的三份制度做语料，搭完整 RAG：解析→切分（带条款元数据）→向量化→
混合检索（关键词+向量）→可选 ReRank→让模型带**条款出处**回答。
验证："单一来源采购超过多少钱要专家论证？""高风险供应商能不能准入？"
**你将真正理解**：切分策略决定检索质量；为什么纯向量搜"文号/50万"会翻车（所以真项目必须混合检索）；合规场景为什么答案必须带出处。

### 第 7 关 · MCP + 外部征信接入 → 供应商风险审核（2~3 个晚上）
**造的零件**：MCP server/client / 外部数据接入 ｜ **对标**：工具集 MCP + 风险评估插件
用官方 `mcp` SDK 把"征信查询、涉诉名单查询"做成一个 MCP server，让你的 harness 作为 client 接入。
用它实现**供应商风险智能审核**：查工商/司法/经营/舆情 + 命中涉诉名单 → 风险分级 + 处置建议。
**关键纪律**：输出要区分"外部数据命中的事实"和"AI 综合分析建议"。
**你将真正理解**：MCP 解决什么（工具写一次、多 agent 复用、供应商解耦）；为什么风险结论不能把模型推断当权威事实。

### 第 8 关 · 毕业项目：三大采购场景 + 工作流 + 权限 + 评测（一个周末）
**造的零件**：工作流编排 / 权限过滤 / 评测 ｜ **对标**：采购智能场景 + 权限安全 + 验收指标
把前 7 关拼起来，用**工作流**串联多步骤，落地三个场景：
1. 单一来源智能审核（查金额→检索制度→生成理由→合规提示→回填，人工确认）
2. 供应商智能推荐（结构化评分 + 语义匹配，给可解释排序和扣分项）
3. 供应商风险审核（复用第 7 关）
再加**权限过滤**（不同单位只能查本单位供应商）和**评测集**（20 题跑分，改一版重测）。
**验收**：三个场景跑通，回答带出处，越权数据查不到，评测集 ≥ 70%。

---

## 四、怎么"边上手边学"

1. **每关先自己写，卡住再问 AI**。目标是理解原理不是完成代码。正确姿势：让 Cursor 先讲概念和 API，你写主干，卡住贴报错让它诊断——正好就是你试讲里教学生的方法。
2. **每关结束回答任务卡的思考题**，答不上来说明白过了，回去翻代码。
3. **刻意制造故障**：把工具描述改烂看它变傻、把步数上限调成 2 看它被截断、喂脏数据看 RAG 怎么翻车。坏例子比好例子教得多——也正好帮你提前理解"客户历史数据不标准 RAG 为什么难"。
4. **写学习日志**：每关在 `notes.md` 记 3 条"今天理解了什么"。这些将来就是你给客户讲方案的素材。
5. **进度参考**：每周 3 晚 + 周末半天，约 5~6 周毕业。第 2、3、6 关别赶，那是根基和客户项目核心。

---

## 五、项目结构

```
agent-lab/
├── 学习指南.md              ← 本文件（总纲）
├── pyproject.toml           ← 依赖清单（uv 管理）
├── 采购数据/                ← 数据底座：供应商库 / 制度 / 外部征信（见其 README）
├── stage1_chat/             ← 第1关：采购助手雏形（含起步代码）
├── stage2_tools/            ← 第2关：agent 循环 + 查供应商库
├── stage3_harness/          ← 第3关：harness + MaaS 模型网关
├── stage4_skills/           ← 第4关：提示词/技能管理
├── stage5_memory/           ← 第5关：会话记忆与上下文
├── stage6_rag/              ← 第6关：制度知识库 RAG
├── stage7_mcp/              ← 第7关：MCP + 征信风控
└── stage8_capstone/         ← 第8关：三大采购场景毕业项目
```

每个 stage 的 `README.md` 都是任务卡：本关目标 / 对标真项目 / 前置阅读 / 动手任务（分步）/ 验收标准 / 思考题 / 常见坑。

> **关于参考实现**：每关目录下的 `solution.py`（以及第3/6/7关的 `gateway.py`/`harness.py`/`ingest.py`/`rag.py`/`server.py`）
> 是**参考答案**，已在本机实跑验证通过。建议你先照 README 自己写，卡住了再对照。
> 环境已装好、模型已拉好、每关都验证过——详见 `运行说明与验证结果.md`（含每关的运行命令和验证结果）。

现在，打开 `stage1_chat/README.md`，开始第 1 关。
