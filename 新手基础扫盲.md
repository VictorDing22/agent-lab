# 新手基础扫盲（进关卡前先读这一篇）

> 面向完全没接触过"本地大模型 / agent 开发"的你。用大白话把这套东西是什么、
> 我给你装了哪些工具、每个名词啥意思讲清楚。读完再去看 `学习指南.md` 和各关就不懵了。
> 遇到不认识的词，回来这里查。

---

## 一、这套东西整体在干嘛（一句话）

在**你自己的电脑上**跑一个 AI，让它能查采购数据、答制度、审风险——
而不是把数据发给网上的 ChatGPT。这就是"本地部署 / 私有化"，也是你客户（西高院）要的东西。

我们用小规模、免费的方式，在你的 Mac 上把这件事完整做一遍。

---

## 二、我给你装了哪些东西，各是什么

想象你要开一家"AI 面馆"，需要几样东西：

| 我装的东西 | 大白话是什么 | 类比 |
|---|---|---|
| **Ollama** | 一个能在本地下载并运行大模型的软件 | 厨房 + 灶台：把"大厨"请进来、让他开火干活 |
| **模型**（qwen3:8b 等） | 真正会"思考、说话"的 AI 大脑，是一个几 GB 的文件 | 大厨本人：装进厨房才能炒菜 |
| **Python** | 一门编程语言，我们用它写"指挥 AI 的代码" | 服务员：把客人的需求传给大厨，再把菜端出来 |
| **uv** | 管理 Python 和它依赖库的工具 | 后勤：负责把服务员要用的餐具、材料备齐 |
| **依赖库**（openai、numpy…） | 别人写好的现成代码包，拿来即用 | 现成的调料包：不用自己从头熬 |

你在关卡里写的 Python 代码，本质就是那个"服务员"——负责把你的问题交给模型、执行工具、把结果整理好。

---

## 三、必懂的基础概念（12 个，够用了）

### 1. 大模型 / LLM
就是 ChatGPT、通义千问、DeepSeek 这类会聊天的 AI。英文 Large Language Model，简称 LLM。
本质：给它一段文字，它预测接下来该说什么。**它不会真的"执行"任何操作**，只会输出文字。

### 2. 本地部署 / 私有化
把模型下载到自己的电脑/服务器上运行，数据不出门。
对比：用网页版 ChatGPT，数据要发到 OpenAI 的服务器。央企、政府因为保密要求，必须本地部署。

### 3. Ollama
让"本地部署"变简单的软件。一条命令 `ollama pull qwen3:8b` 就能把一个大模型下载下来，
再一条命令就能让它跑起来对外提供服务。**它现在已经在你电脑后台常驻运行了。**

### 4. 模型的名字，比如 `qwen3:8b`
- `qwen3` = 通义千问第 3 代（阿里出的开源模型）
- `8b` = 80 亿参数（parameter）。参数越多一般越聪明，但越大越吃内存、越慢。
- 我还装了 `qwen3:0.6b`（6 亿参数，小而快，答案质量差些，赶时间时用）
- `bge-m3` = 一个专门做"向量化"的模型（见第 9 条），不用来聊天

### 5. 服务 / 端口 / `localhost:11434`
Ollama 跑起来后，会在你电脑内部开一个"窗口"等着别人来提问，这个窗口地址是
`http://localhost:11434`。
- `localhost` = 本机（就是你这台电脑自己）
- `11434` = 端口号，相当于"几号窗口"
你的 Python 代码就是通过访问这个地址来和模型对话的。

### 6. API / 接口
两个程序之间约定好的"对话格式"。你的代码按约定格式把问题发给 Ollama，Ollama 按约定格式把答案返回。
就像去银行填固定格式的单子办业务。

### 7. OpenAI 兼容接口（重要）
Ollama 特意把自己的接口做得和 OpenAI（ChatGPT 的公司）**一模一样**。
好处：你现在写的代码，将来把地址从 `localhost:11434` 换成客户的千问/昇腾服务器地址，
**代码基本不用改**就能用。所以现在练的东西不会白费。

### 8. token（词元）
模型不是按"字"算，而是按 token 算。中文大约 1 个字 ≈ 1~2 个 token。
为什么要关心它：
- 模型一次能处理的 token 有上限（见第 10 条）
- 商用模型按 token 收费
一段话"消耗了多少 token"≈ 它有多长。

### 9. Embedding / 向量 / 向量化
把一段文字变成一串数字（比如 1024 个小数），意思相近的文字，数字也相近。
用途：**按"意思"搜索**。比如你搜"多少钱要专家论证"，它能找到写着"50万元以上须论证"的条款，
即使字面不完全一样。`bge-m3` 就是干这个的。（第 6 关会用到）

### 10. 上下文窗口（context window）
模型的"短期记忆容量"，即一次最多能看多少 token。超了就得把旧内容删掉或压缩。
所谓 AI"记得刚才聊的"，其实是每次都把之前的对话原样重发给它——它本身过目即忘。（第 5 关讲）

### 11. Agent（智能体）
不只会聊天，还能"自己决定调用工具"的 AI 程序。
比如你问"S005 靠谱吗"，它会自己决定：先调"查供应商"工具拿数据，再总结回答。
Agent = 大模型 + 一堆工具 + 一个让它循环"思考→调工具→再思考"的程序。（第 2 关亲手写）

### 12. RAG（检索增强生成）
先从你的资料库里搜出相关内容，再让模型"照着这些内容回答"。
好处：答案有依据、能标出处、不容易瞎编。采购制度问答就是靠它。（第 6 关）

---

## 四、命令行基础（你会用到的就这几个）

命令行 = 那个黑框框（终端 Terminal），用打字代替点鼠标。

```bash
# cd = 进入某个文件夹（change directory）
cd /Users/dingsaier/Desktop/文件生成器/agent-lab

# uv run python xxx.py = 用项目环境运行一个 Python 文件
uv run python stage1_chat/chat.py

# 程序运行中想退出：按 Ctrl + C；聊天程序里按约定输入 /quit
```

- 命令里的**空格、大小写**要准确。
- 路径有中文没关系，但整段最好别打错字。
- 复制命令时，`#` 开头的是注释，可以不管。

---

## 五、遇到问题怎么办（常见 3 个）

1. **报错 `connection refused` 或 `无法连接 11434`**
   → Ollama 服务没在跑。运行 `open -a Ollama`，或在启动台点一下 Ollama 图标，等几秒再试。

2. **第一次提问很慢 / 卡住不动**
   → 模型第一次要从硬盘加载进内存，正常现象，等十几秒。之后就快了。
   → 电脑配置一般的话，用小模型：命令前加 `CHAT_MODEL=qwen3:0.6b`。

3. **`command not found: uv`（或 ollama）**
   → 新开一个终端窗口再试（新窗口才会加载新装的命令）。还不行就告诉我。

---

## 六、这套练习 和 客户真项目 的对应

你在本地用的小东西，就是客户项目里那些要花钱买、要部署服务器的大东西的"迷你替身"：

| 你本地用的 | 西高院真项目里的 |
|---|---|
| Ollama | 私有化部署的模型服务（昇腾服务器 / MaaS 平台） |
| qwen3:8b | 千问大模型 |
| bge-m3 | Qwen3-Embedding / BGE-M3 向量模型 |
| 你写的模型网关代码（第3关） | MaaS / 模型网关 |
| `采购数据/` 里的 json 和 md | 供应商信息库 + 制度知识库 |
| SQLite 存向量（第6关） | Milvus / Elasticsearch 向量库 |

原理完全一样，只是规模和钱不一样。**你把小的搞懂了，大的就只是"更多、更贵、更复杂的工程"，没有新魔法。**

---

## 七、接下来怎么走

1. 读这篇（你已经在读了）
2. 读 `学习指南.md`（有全局地图和概念速查表）
3. 读 `运行说明与验证结果.md`（每关怎么跑，我已验证过）
4. 打开 `stage1_chat/README.md`，开始第 1 关

不认识的词，随时回这篇查。祝上手顺利。
