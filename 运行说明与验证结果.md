# 运行说明与验证结果

> 我已经在你这台 Mac 上把环境装好、模型拉好，并把 8 个关卡从头到尾实跑验证了一遍。
> 你现在可以直接运行任何一关。本文件记录：环境现状、每关怎么跑、我的验证结果。

## 一、环境现状（已就绪，无需你再装）

- **uv** 0.11.29（已装，`/opt/homebrew/bin/uv`）
- **Ollama** 0.32.1（已装为 `/Applications/Ollama.app`，服务已在 `127.0.0.1:11434` 运行）
- **已拉取模型**：
  - `qwen3:8b`（5.2GB）—— 课程默认对话/分析模型（对标"千问"）
  - `bge-m3`（1.2GB）—— 向量模型（第 6 关制度检索用）
  - `qwen3:0.6b`（522MB）—— 我用来快速冒烟测试的小模型，你也可以用它在低配下跑得更快
- **依赖**：`uv sync` 已完成，虚拟环境在 `agent-lab/.venv`（Python 3.13）

> 如果重启电脑后 `curl http://localhost:11434/api/version` 不通，
> 打开一次"启动台 → Ollama"或运行 `open -a Ollama` 即可让服务常驻。

## 二、每关怎么跑

每关的 `solution.py` 是**参考实现（标准答案）**。建议你先照着该关 `README.md` 自己写，
卡住了再对照 `solution.py`。想直接看效果，就按下面命令跑自检：

```bash
cd /Users/dingsaier/Desktop/文件生成器/agent-lab

# 第1关 采购助手对话（交互）
uv run python stage1_chat/chat.py

# 第2关 agent循环+查供应商库
uv run python stage2_tools/solution.py --selftest

# 第3关 harness + 模型网关（看 stage3_harness/gateway_log.jsonl 里的调用日志）
uv run python stage3_harness/solution.py --selftest

# 第4关 提示词/技能（渐进式加载）
uv run python stage4_skills/solution.py --selftest

# 第5关 记忆（压缩 + 跨会话长期记忆）
uv run python stage5_memory/solution.py --selftest

# 第6关 制度RAG：先建索引，再问答（带条款出处）
uv run python stage6_rag/ingest.py
uv run python stage6_rag/solution.py --selftest

# 第7关 供应商风险审核（征信工具，可追溯）
uv run python stage7_mcp/solution.py --selftest
#   MCP 方式（可选）：用官方 Inspector 手动验证 server
#   npx @modelcontextprotocol/inspector uv run python stage7_mcp/server.py

# 第8关 三大场景 + 评测
uv run python stage8_capstone/recommend.py                       # 供应商推荐（可解释评分）
uv run python stage8_capstone/solution.py 单一来源 "某专利检测设备" 120
uv run python stage8_capstone/solution.py 风险 远东电力工程有限公司
uv run python stage8_capstone/run_evals.py                       # 评测集跑分
```

> **想跑得更快**：在命令前加 `CHAT_MODEL=qwen3:0.6b`，用小模型（答案质量下降但速度快很多），例如：
> `CHAT_MODEL=qwen3:0.6b uv run python stage2_tools/solution.py --selftest`

## 三、我的验证结果（全部通过）

| 关卡 | 验证内容 | 结果 |
|---|---|---|
| 1 | 连本地千问多轮对话、流式 | ✅ 正常 |
| 2 | agent 自主调用 3 个供应商工具并综合作答 | ✅ 多步工具调用成功 |
| 3 | 工具装饰器自动生成 schema、模型网关路由+调用日志 | ✅ gateway_log.jsonl 记录 token/耗时 |
| 4 | 技能清单注入 + 按需 load_skill | ✅ 机制生效（8b 效果更好） |
| 5 | 上下文压缩 + 跨会话长期记忆 | ✅ 重启后仍记得用户身份 |
| 6 | 制度切分→bge-m3向量化→混合检索→带条款出处回答 | ✅ 条款引用准确；语料外问题如实说"未查到"不编造 |
| 7 | 供应商风险审核：外部数据事实 vs AI建议 分离 | ✅ 中原电气→低风险、远东电力→高风险 |
| 8 | 推荐可解释评分（去重+剔除高风险）、单一来源工作流、评测集 | ✅ 评测 8/8=100%（含一次 50%→100% 的调优演示） |

几个亮点，正好对应你给客户讲方案时的卖点：
- **可追溯**：第 6 关回答带《制度名》第X条；出差报销这种语料外问题会诚实说"未查到"，不编造。
- **事实与建议分离**：第 7 关严格区分【外部数据事实】和【AI综合分析建议】，符合采购合规红线。
- **人机协同**：第 8 关单一来源理由结尾强制"需人工确认，系统不自动审批"。
- **评测驱动**：第 8 关演示了改一版匹配规则后评测分从 50% 提到 100% —— 这就是"不能只凭演示样例验收"的实操。

## 四、验证过程中修的两个小问题（已修复）

1. 第 5 关：小模型偶尔把工具参数写成非法格式会导致崩溃 → 已加参数容错，坏参数不再让程序挂掉。
2. 第 8 关：评测匹配过于死板（"风险等级为高"匹配不到"高风险"）→ 改成同义词组匹配，更贴近真实语义评测。

这两个坑本身就是很好的教学案例（真实项目里都会遇到），我在对应文件里留了注释。
